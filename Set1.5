// Array is Palindrome or Not
#include<bits/stdc++.h>
using namespace std;

int main()
{
    cout<<"Enter 5 Integers in the Array :";
    int a[5],b[5];
    for(int i=0;i<5;i++)
    {
        cin>>a[i];
    }
    
    int i=0,j=4,temp;
    
     int count=0;
    while(i < j)
    {
        if(a[i]==a[j])
        {
            count++;
        }
        i++,j--;
    }
    
  
    if(count==2) cout<<"Array is Palindrome";
    else cout<<"Array is not Palindrome";
    
    return 0;
}

#include <iostream>
using namespace std;

int main() {
    int n, target, index = -1;
    cout << "Enter the size of the array: ";
    cin >> n;
    
    int arr[n];
    cout << "Enter " << n << " elements: ";
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    
    cout << "Enter the number to search: ";
    cin >> target;
    
    for (int i = 0; i < n; i++) {
        if (arr[i] == target) {
            index = i;
            break;
        }
    }
    
    cout << "Index: " << index << endl;  // Prints -1 if not found
    return 0;
}
